function regionIDs = getRegionIDsForAllWsFaces(ws,setOfRegions,edges2pixels)
% For all the faces in WS, get the corresponding regionID.
% RegionID is the sequence number of each region in setOfRegions

numWsFaces = max(max(ws));

for i=1:numWsFaces
    % get internal pixels
    
    % get edge pixels
    
    % get edges
    
    % get regionID
end
