function fm = edgeFeatures(edgepixels,OFR,fm_membrane)

numEdges = size(edgepixels,1);
numOFRdim = size(OFR,3);

% initialize feature matrix
fm = zeros(numEdges,numOFRdim);
for i=1:numEdges
    % get all edge pixels
    % get OFR for each pixel (all dimensions)
    
    
end

