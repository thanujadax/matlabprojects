function createValidationFibStack()

% input tiff stack path:

% specify which direction we want to interleave. should be either along the
% x axis or the y axis since we already know 


% write it to a tif file so that